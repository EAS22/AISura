node_modules/
dist/
src-tauri/target/
ACCESS_TOKENS.md
*.db
.DS_Store

.worktrees/

.superpowers/

# Generic env file ignore (in case future builds use them).
.env
.env.local
.env.*.local

# Local docx fixtures used by docxCleaner integration tests; they may
# contain real village data and should not be committed.
SKU.docx
*.fixture.docx

# Local backup test outputs — may contain real warga / template data,
# never commit. App emits these next to the working directory when the
# user picks the default save location.
aisura-warga-backup-*.json
aisura-templates-backup-*.zip
